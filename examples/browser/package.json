{
  "name": "browser",
  "private": true,
  "scripts": {
    "build": "cd ../../browser && npm run build && cd ../examples/browser && cp ../../browser/index.mjs .",
    "start": "serve"
  },
  "dependencies": {
    "serve": "^11.3.0"
  }
}
